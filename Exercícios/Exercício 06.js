// --> Exercício 6. Crie duas variáveis para armazenar nomes de frutas.

// Armazena o valor em duas variáveis "fruit1" e "fruit2";
const fruit1 = 'Banana';
const fruit2 = 'Morango';

// --> a. Escreva uma estrutura condicional que verifica se as duas frutas são iguais e imprima uma mensagem se forem, ou uma mensagem diferente se forem diferentes.
// Verifica se o as frutas são iguais e imprime conforme o resultado.
if (fruit1 === fruit2) {
  console.log(`As frutas ${fruit1} e ${fruit2} são iguais.`);
}
else {
  console.log(`As frutas ${fruit1} e ${fruit2} são diferentes.`);
}
